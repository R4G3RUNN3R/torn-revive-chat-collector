const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('node:fs'); const os=require('node:os'); const path=require('node:path');
const {loadReleaseManifest}=require('../../src/release/registry');
const manifest={latestVersion:'0.4.0',minimumVersion:'0.3.0',releasedAt:'2026-08-26T12:00:00.000Z',releaseNotes:'Release.',gitCommit:'0'.repeat(40),automatic:{installUrl:'https://reviverelay.voidsmithindustries.com/install/reviverelay-auto.user.js',metaUrl:'https://reviverelay.voidsmithindustries.com/install/reviverelay-auto.meta.js',sha256:'a'.repeat(64)},manual:{installUrl:'https://reviverelay.voidsmithindustries.com/install/reviverelay-manual.user.js',sha256:'b'.repeat(64)},mandatory:false};
test('loads validates and deep-freezes release manifest',()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'rr-release-')); const file=path.join(dir,'manifest.json'); fs.writeFileSync(file,JSON.stringify(manifest)); const registry=loadReleaseManifest(file); assert.equal(registry.latestVersion,'0.4.0'); assert.equal(Object.isFrozen(registry),true); assert.equal(Object.isFrozen(registry.automatic),true); registry.automatic.sha256='x'; assert.equal(registry.automatic.sha256,'a'.repeat(64));});
test('missing or invalid enabled manifest fails',()=>{assert.throws(()=>loadReleaseManifest('/definitely/missing/manifest.json'),/manifest/i); const dir=fs.mkdtempSync(path.join(os.tmpdir(),'rr-release-')); const file=path.join(dir,'manifest.json'); fs.writeFileSync(file,'{}'); assert.throws(()=>loadReleaseManifest(file),/manifest/i);});
