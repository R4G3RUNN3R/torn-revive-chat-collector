const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const {loadReleaseRegistry}=require('../../src/release/registry');
function manifest(channel='review'){return {latestVersion:'0.6.0',minimumVersion:'0.6.0',buildTimestamp:'2026-09-07T12:00:00.000Z',releaseNotes:'Review.',gitCommit:'0'.repeat(40),releaseChannel:channel,sha256:'a'.repeat(64),apiCompatibility:{minimum:1,current:1},install:{installUrl:`https://reviverelay.voidsmithindustries.com/releases/${channel}/0.6.0/ReviveRelay-0.6.0.user.js`,metaUrl:`https://reviverelay.voidsmithindustries.com/releases/${channel}/0.6.0/ReviveRelay-0.6.0.meta.js`},mandatory:false};}

test('release registry loads and freezes distinct validated review and stable manifests',()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'rr-reg-'));fs.writeFileSync(path.join(dir,'review.json'),JSON.stringify(manifest('review')));fs.writeFileSync(path.join(dir,'stable.json'),JSON.stringify(manifest('stable')));const registry=loadReleaseRegistry({review:path.join(dir,'review.json'),stable:path.join(dir,'stable.json')});assert.equal(registry.review.releaseChannel,'review');assert.equal(registry.stable.releaseChannel,'stable');assert.equal(Object.isFrozen(registry),true);assert.equal(Object.isFrozen(registry.review),true);});
