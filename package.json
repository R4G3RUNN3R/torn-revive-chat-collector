{
  "name": "torn-revive-chat-collector",
  "version": "0.4.0",
  "private": true,
  "description": "ReviveRelay public revive-candidate client and isolated direct-request backend for Torn.",
  "scripts": {
    "test": "npm run test:client && npm run test:server",
    "test:client": "node --test test",
    "test:server": "npm --prefix server test",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build && node --check dist/reviverelay-auto.user.js && node --check dist/reviverelay-manual.user.js",
    "release:client": "npm run check && node scripts/release-client.js"
  },
  "engines": {
    "node": ">=20"
  }
}
