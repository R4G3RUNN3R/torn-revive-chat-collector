{
  "name": "torn-revive-chat-collector",
  "version": "0.1.0",
  "private": true,
  "description": "Research collector for visible Torn Chat 2.0 messages, with local buffering and optional Google Sheets sync.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build && node --check dist/torn-revive-chat-collector.user.js"
  },
  "engines": {
    "node": ">=20"
  }
}
